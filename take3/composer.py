import constraints
import flow



def constraints_to_flow(cons):
    if isinstance(cons)



def compose(root_of_constraints):
    depsol = constraints.DependencySolver(root_of_constraints)
    root



